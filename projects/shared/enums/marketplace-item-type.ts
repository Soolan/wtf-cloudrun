export enum MarketplaceItemType {
  Api = 'api',
  Team = 'team',
  Agent = 'agent',
  Playbook = 'playbook',
  Company = 'company',
}

export enum InstallStatus {
  Installed = 'installed',
  Uninstalled = 'uninstalled',
}
