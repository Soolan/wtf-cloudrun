export enum TopicStatus {
  Draft = 'Draft',
  Review = 'Review',
  Reject = 'Reject',
  Accept = 'Accept',
}

export enum TopicAction {
  Add = 'Add',
  Edit = 'Edit',
  Delete = 'Delete',
  Generate = 'Generate',
}
