export enum MemberRank {
  Board = 'Board',
  CSuite = 'CSuite',
  Department = 'Department', // as in department employee
}

export enum EntityType {
  Human = 'Human',
  AI = 'AI',
}

export enum InvitationStatus {
  Pending = 'Pending',
  Accepted = 'Accepted',
  Rejected = 'Rejected',
  Expired = 'Expired',
}
