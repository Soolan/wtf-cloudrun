export enum MemberLevel {
  Board = 'Board',
  CSuite = 'CSuite',
  Department = 'Department',
}

export enum MemberSource {
  Human = 'human',
  AI = 'ai',
}

export enum InvitationStatus {
  Pending = 'pending',
  Accepted = 'accepted',
  Rejected = 'rejected',
  Expired = 'expired',
}
